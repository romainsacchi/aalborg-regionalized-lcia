# Fallback pip/venv environment for participants who cannot use Anaconda, conda, or Miniforge.
# Use with Python 3.11 from the repository root:
# python -m pip install -r envs/requirements_venv.txt

pip<26
setuptools<81
wheel

bw2calc>=2.5.0
bw2data>=4.7
bw2io>=0.9.17
bw2analyzer>=0.11.8
bw2parameters>=1.1.0
bw_processing>=1.6
stats_arrays

numpy>=1.24.4,<2.0.0
pandas<3.0.0
scipy<1.14.0
sparse>=0.14.0,<0.18
bottleneck

jupyterlab
ipykernel
openpyxl
matplotlib
plotly
networkx
seaborn
prettytable

constructive_geometries>=1.0.0
country_converter
ecoinvent_interface
cryptography
datapackage
platformdirs
pyarrow
pycountry
python-dotenv
pyyaml
requests
schema
xarray<=2024.2.0
tqdm
fair
pyvis

edges==1.4.0
pypardiso; platform_system == "Windows"
multifunctional==1.1.1
bw_simapro_csv==0.4.3
d3blocks==1.2.8
polyviz==1.0.4

PySide6==6.9.3
PySide6-Addons==6.9.3
activity-browser

# The macOS conda recipe includes scikit-umfpack. In a venv, installing it
# usually requires system SuiteSparse/UMFPACK headers, so it is not listed as a
# portable pip requirement. If bw2calc warns about missing optimized solvers,
# continue unless a calculation cell actually fails.
